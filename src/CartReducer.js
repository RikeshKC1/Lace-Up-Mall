import { createSlice } from "@reduxjs/toolkit";

const initialCart={
    cart:{}
}
export const CartSlice=createSlice({
    name:"cart",
    initialState:initialCart,
    reducers:{
        
    }

})